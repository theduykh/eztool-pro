services:
  eztool-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: eztool-web
    restart: always
    ports:
      - "80:3000"
    environment:
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
