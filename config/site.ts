/**
 * Canonical production origin. Used for `metadataBase`, hreflang alternates,
 * the sitemap and robots.txt. Override here if the domain changes.
 */
export const SITE_URL = "https://eztool.pro";
